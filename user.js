var mongo = require('mongodb').MongoClient;


exports.getUserInfo = function (userId) {
    user = {
        data : "requete"
    };
    user.refreshDate = Date();
    return user;
}

function login(userId, hashedPw) {
    userData = 
}